---
description: 更新派蒙助手并安全发布新版本到私有 GitHub 仓库
argument-hint: "<本次更新内容>"
---

本次更新内容：

`${ARGUMENTS:-（未提供）}`

你正在维护当前工作区中的“派蒙助手”。请使用中文沟通，并把本次更新从代码实现一直完成到私有 GitHub 仓库发布。

如果上面没有提供具体更新内容，只询问“这次要更新什么功能或修复什么问题？”，然后停止。不要自行猜测需求。

本模板的调用代表用户授权你在当前派蒙助手仓库中完成与本次需求直接相关的代码、测试、文档、版本号、Git 提交、全新版本标签和非强制推送。它不授权你公开仓库、强制推送、改写历史、删除或移动旧标签、覆盖用户无关改动、提交凭据，或上传未要求的文件。

## 必须遵守的发布原则

1. 保留所有旧提交和旧版本标签。任何已有的 `vX.Y.Z` 标签都不可删除、重建或改指向。
2. 禁止使用 `git push --force`、`git push --force-with-lease`、`git reset --hard`、破坏性 checkout 或其他历史改写操作。
3. GitHub 仓库必须保持私有。推送前尽量通过认证 API 确认 `private: true`；无法确认时停止外部推送并报告，不得擅自改变可见性。
4. 除项目已跟踪的 `.pi/prompts/update-paimon.md` 外，不提交 `.pi/` 中的其他内容，也不提交 `.venv/`、缓存、`build/`、`dist/`、日志、认证文件、令牌、密码或其他本机文件。
5. 默认只推送源码、测试、文档和打包配置。若本次更新内容明确要求上传 EXE，应把构建产物作为对应 GitHub Release 的附件，不要把 `dist/PaimonAssistant.exe` 加入 Git 历史。
6. 测试、构建或冒烟验证失败时，不得创建发布提交、标签或推送；先修复本次改动导致的问题。无法修复时保留现场并清楚报告。
7. 遇到远端分叉、合并冲突、权限不足、已有同名标签或不明确的用户改动时，停止危险步骤并说明阻塞，不得覆盖。

## 执行流程

### 1. 发布前检查

- 确认当前目录是派蒙助手仓库，读取项目规则、README、需求文档和相关源码。
- 检查 `git status --short --branch`、当前分支、远端地址、最近提交和全部版本标签。
- 执行 `git fetch origin --prune --tags`，确认本地与远端基线；不要自动丢弃或覆盖现有未提交改动。
- 区分本次需求相关改动和用户已有的无关改动，只处理并提交本次范围。
- 检查当前版本来源，包括 `paimon_assistant/__init__.py`、README、CHANGELOG 和最新可达的 SemVer 标签。

### 2. 自动确定新版本号

遵循 SemVer，并选择尚未使用的新标签：

- `PATCH`，如 `v0.1.0 -> v0.1.1`：修复错误、安全问题、兼容性优化或仅文档/维护更新。
- `MINOR`，如 `v0.1.0 -> v0.2.0`：增加向后兼容的新功能。
- `MAJOR`，如 `v0.2.0 -> v1.0.0`：存在明确的不兼容变更。

根据实际改动自动判断。若是否属于破坏性变更会影响用户数据、配置或既有使用方式，先向用户确认，不要自行发布重大版本。

### 3. 实现更新

- 先阅读相关实现和测试，明确行为与验收条件。
- 按现有项目结构完成最小且完整的实现，不进行无关重构。
- 为新增行为或修复补充针对性测试；缺陷修复应尽量先建立可复现测试。
- 更新必要的用户说明和需求文档，不虚构尚未实现或尚未验证的功能。

### 4. 更新发布资料

- 将 `paimon_assistant/__init__.py` 中的 `__version__` 更新为新版本。
- 更新 README 中的当前版本和受影响说明。
- 在 CHANGELOG 顶部新增本次版本，写明真实日期、功能或修复、验证结果和仍存在的限制；保留所有旧版本记录。
- 确认代码版本、README、CHANGELOG、提交信息和标签一致。

### 5. 完整验证

根据当前环境执行项目可用的全部验证，至少包括：

- Qt offscreen 完整自动化测试。
- 源码 `--smoke-test`。
- PyInstaller 干净打包。
- 打包后的 EXE `--smoke-test`。
- `git diff --check`。
- 敏感信息扫描和待提交文件清单检查。

没有真实串口或 com0com 时，不得声称真实串口验收通过；只报告自动化验证结果和未执行项。

### 6. 提交和发布

只有前述检查全部通过后才执行：

1. 仅暂存本次发布相关文件，并检查完整 staged diff。
2. 创建提交：`release: Paimon Assistant vX.Y.Z`，正文概括本次功能、修复和验证。
3. 创建全新的注释标签 `vX.Y.Z`，标签说明概括该版本功能与验证；不得复用已有标签。
4. 再次 `git fetch origin --prune --tags`，确认远端没有需要先处理的新提交，且推送是正常快进。
5. 使用非强制原子推送，将当前发布分支和新标签一起推送到 `origin`；禁止任何形式的 force。
6. 推送后核对远端分支、新标签解引用后的提交、仓库私有状态和本地工作树。
7. 如果明确要求发布 EXE，再为新标签创建 GitHub Release 并上传本次构建的 EXE，附带 SHA-256；不得覆盖旧 Release 的附件。

## 最终回复

简洁报告以下内容：

- 新版本号、提交短 SHA 和标签。
- 本次真正实现的功能或修复。
- 测试、打包和冒烟验证结果，以及未执行的真实设备测试。
- 私有仓库地址和推送结果。
- 本地工作树是否干净。
- 明确说明旧版本标签仍保留，本次发布没有覆盖旧版本。
