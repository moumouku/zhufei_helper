# 派蒙助手串口接收上下文

本上下文统一描述派蒙助手串口接收链路中的领域术语，避免数据帧、接收事件和解析等概念混用。

## Language

**数据流**：从串口连续到达的原始字节序列。

**数据帧**：数据流中以 `\r\n` 作为结束边界的一条完整数据。

**载荷**：数据帧中不包含末尾 `\r\n` 的原始字节内容。

**原始帧**：包含末尾 `\r\n` 的完整数据帧字节内容。

**接收事件**：一条数据帧完成后形成的、携带该帧接收时间和数据内容的记录。

**解析**：从连续数据流中识别数据帧边界，并将完整数据帧转换为接收事件；不对载荷内部的业务字段作额外解释。

**日志**：按接收事件的发生顺序保存数据内容及其接收时间的持久记录。

**超长数据帧**：在遇到 `\r\n` 前，载荷长度超过约定上限而不能作为正常接收事件处理的数据帧。

**日志故障**：日志文件不可写的状态；该状态不改变串口接收、接收事件生成、事件队列或界面显示等核心功能。
