# 变更记录

## v0.4.0 - 2026-09-17：REQ-0004 接收解析模式选择

### 已实现功能

- 新增“接收解析”下拉框（`parse_mode_combo`），可选 `按 \r\n 分帧`（默认）与 `原始字节`，不记住上次选择
- “原始字节”模式：串口读取块到达即进入原始字节历史并显示，不做分帧；单独 `\r`、单独 `\n`、`\r\n` 均作为普通内容，不分帧也不分行
- “原始字节”模式下文本/HEX 与 UTF-8/GBK 切换全量重绘；跨读取块被拆开的多字节字符保持待定，不提前输出替换字符
- “原始字节”模式不产生接收事件、不写 `RX` 日志、不做超长帧判定；时间戳开关在该模式下禁用（显示不带时间戳前缀）
- 切换接收解析模式会重置分帧器未完成尾部，避免切换期间的字节与切换前遗留尾部拼成错误事件
- 切换模式保留两种已积累历史；两种通道互不污染（原始字节不会在切回分帧后变成事件）
- `SerialController` 新增 `raw_queue`（只放 `bytes`）与 `set_raw_mode(enabled)`；模式标记在控制器上持久化，重新打开连接沿用；`reset_receive_session()` 同时清空事件、原始字节与诊断三个队列
- 清空同时清除事件历史与原始字节历史
- 分帧模式的全部 REQ-0003 行为（严格分帧、时间戳、`RX` 日志、30 日保留、日志故障隔离、清空线性化）保持不变
- 新增真实 com0com 端到端验收脚本 `tests/test_manual_com0com_e2e.py`，默认跳过，设置 `PAIMON_COM0COM_E2E=1` 后覆盖两种接收模式、发送行为与日志落盘

### 验证

- 自动化测试：`330 passed, 6 skipped`（跳过项：需符号链接特权 1 条 + 默认跳过的真实串口验收 5 条）
- 真实 com0com 端到端：`PAIMON_COM0COM_E2E=1` 下 `5 passed`（窗口侧 `COM17`，对端 `COM19`，`115200 / 8 / N / 1`）
- 源码冒烟测试：`main.py --smoke-test` 通过；PyInstaller 单文件构建与 `PaimonAssistant.exe --smoke-test` 通过

## v0.3.0 - 2026-09-17：REQ-0003 串口接收事件、时间戳与日志

### 已实现功能

- 新增无 Qt 依赖的接收分帧模块，以连续 `0D 0A` 作为唯一帧结束边界；单独的 `\r`、单独的 `\n` 和 `\r\r\n` 按严格规则处理，支持边界跨读取块和一次读取多帧
- 新增不可变 `ReceivedEvent`（`received_at_ms` / `payload` / `raw_frame`），在识别到结束边界的同一处理点用可注入毫秒时间源记录时间戳
- 每个连接使用独立会话的分帧器和事件队列，reader 只发布完整事件，不再向界面传裸字节；分帧诊断走独立线程安全队列
- 未完成帧载荷超过 1 MiB 时丢弃当前帧并持续丢弃到下一个 `\r\n`；恰好 1 MiB 仍有效，同一段溢出只提示一次
- 接收显示改为事件历史渲染：文本模式显示 `payload`，HEX 模式显示完整 `raw_frame`；时间戳前缀格式为 `[HH:mm:ss.SSS] `
- 新增“时间戳”显示开关（`QCheckBox`），启动默认开启、不持久化；切换开关、接收模式或编码时基于事件历史重绘
- 新增接收日志服务：按事件完成时的本地日期写入 `%LOCALAPPDATA%\PaimonAssistant\logs\YYYY-MM-DD.txt`（UTF-8），每行 `[HH:mm:ss.SSS] RX <原始帧 HEX>`，写入后及时刷新
- 日志日期只在文件名中出现；跨午夜完成的事件写入次日文件；界面时间戳开关不影响日志内容
- 新增日志保留清理：启动时与跨日首次写日志前删除早于当前日期前 30 天的日期日志，只处理严格命名的普通文件，不按修改时间判断，不删除子目录和其他文件
- 新增“日志目录”入口按钮，自动创建并用资源管理器打开日志目录本身
- 日志首次写入失败时熔断本次运行的后续写入（无内存缓冲、无重试），在界面显示红色提示“日志写入失败，请检查磁盘空间或权限”并写入标准库 `logging`，串口接收、事件生成、队列消费和显示均继续工作
- 接收区新增红色非模态诊断标签，固定提示“接收帧超过 1 MiB，已丢弃”，不使用模态弹窗
- 清空操作接入会话重置：在会话锁内递增代次、重置分帧器、替换接收与诊断队列，原子丢弃清空前未完成尾部、溢出状态和未显示旧事件；清空不删除或修改既有日志
- 发送行为保持原样：文本按当前编码发送、HEX 按输入解析发送，不自动追加 `0D 0A`，发送数据不进入接收事件队列和 `RX` 日志
- 测试套件加入会话级环境隔离与真实用户目录快照护栏，保证测试不写入真实 `%LOCALAPPDATA%`

### 验证

- 自动化测试：`303 passed, 1 skipped`（跳过项为需要创建符号链接特权的保留清理用例，无权限平台自动跳过）
- 覆盖 REQ-0003 §13 全部 18 条验收标准的整合验收矩阵，使用 fake serial、fake 时间源、临时日志目录和 Qt offscreen
- 源码冒烟测试：`main.py --smoke-test` 通过
- 真实串口端到端验收仍需在安装 com0com 或连接实际设备的 Windows 环境中手工执行，默认使用 `COM17 <-> COM19` 和 `115200 / 8 / N / 1`

## v0.2.0 - 2026-08-20：REQ-0002 串口热插拔自动监测

### 已实现功能

- 增加约 1 秒一次的串口轮询和差量列表更新，手动刷新共用同一更新路径
- 新端口在当前选择为空或失效时自动选中枚举顺序中的第一个；有效选择不被打断
- 拔出未选中端口时静默移除；拔出已选中但未连接端口时清空选择且不补选
- 已连接端口连续两次轮询缺失后自动关闭连接并提示，短暂重枚举会取消去抖判定
- 保持读取线程错误立即关闭并提示，监测不会自动打开任何端口

### 验证

- 自动化测试：`181 passed`
- 源码冒烟测试：`main.py --smoke-test` 通过
- PyInstaller 干净构建和 `dist/PaimonAssistant.exe --smoke-test` 均通过
- 测试使用 Qt offscreen、fake port lister 和 fake serial，不依赖真实串口
- 真实串口热插拔验收仍需在安装 com0com 或连接实际设备的 Windows 环境中执行

## v0.1.0 - 2026-08-20

首个可交付版本：派蒙助手 Windows 串口调试上位机。

### 已实现功能

- 自动枚举和刷新可用 COM 口
- 配置波特率、数据位、校验位和停止位，默认 `115200 / 8 / N / 1`
- 支持文本和 HEX 两种接收显示模式
- 支持 UTF-8 和 GBK 编码，并正确处理跨读取块的多字节字符
- 基于原始字节历史重新渲染接收内容，切换模式或编码不丢失数据
- 支持接收区自动滚动和清空，清空时丢弃尚未显示的旧队列数据
- 支持文本和 HEX 两种发送模式
- HEX 输入支持空格、逗号及混合分隔，并拒绝非法字节格式
- 支持点击发送和按回车发送
- 使用独立读取线程和线程安全队列，避免阻塞界面
- 每次串口连接使用独立会话队列，旧连接数据不会注入新连接
- 处理串口打开、读取、写入、断开和端口占用错误，并恢复到可重连状态
- 提供 PyInstaller 单文件窗口程序配置和中文使用说明

### 验证

- 自动化测试：`155 passed`（v0.1.0 发布时）
- 测试使用 Qt offscreen 环境和 fake serial，不依赖真实串口
- com0com 双向串口联调步骤已记录在 `docs/user-manual.md`

### 已知限制

- 仅展示原始字节，不解析具体设备协议
- 接收历史保存在内存中，长时间高流量运行会增加内存占用
- 串口配置暂不持久化
- 真实串口端到端验收需要安装 com0com 或连接实际设备
